# expo-router pulls react-dom@19.2.8 (for web support this app doesn't use),
# which peer-requires a newer react than Expo SDK 57 pins. Without this, every
# npm install fails on an ERESOLVE for a package we never ship.
legacy-peer-deps=true
