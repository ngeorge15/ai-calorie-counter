# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Secrets
.env
*.env
!.env.example

# Node / Expo
node_modules/
.expo/
dist/
*.tsbuildinfo

# Native builds
ios/
android/
*.ipa
*.app

# Model training (datasets are large and reproducible from a script; the
# trained checkpoint itself is small enough (~14MB) to commit directly so
# a clone can run inference without retraining)
model-training/data/
model-training/.venv/

# macOS
.DS_Store
